# Build-time vars, inlined into the client bundle (public).
# Tracked so the AI Studio/Cloud Run build works without a local .env.local.
# Any secrets (CONVEX_DEPLOY_KEY, CLERK_SECRET_KEY) must stay in .env.local / secrets panel.
VITE_CONVEX_URL="https://sleek-squirrel-611.convex.cloud"
VITE_CLERK_PUBLISHABLE_KEY="pk_test_a25vd24tdGFoci00MS5jbGVyay5hY2NvdW50cy5kZXYk"